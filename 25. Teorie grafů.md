#idm, [[grafy1.pdf]], [[grafy2.pdf]]

## Grafy

### Definice grafu

**Graf** je uspořádaná dvojice $G = (V, E)$, kde $V$ je *množina vrcholů* a $E$ je *množina hran* – množina vybraných dvouprvkových podmnožin množiny vrcholů.

> [!note] Značení
> Hranu mezi vrcholy $u$ a $v$ píšeme jako $\{u, v\}$, nebo zkráceně $uv$. Vrcholy spojené hranou jsou *sousední* a hrana $uv$ *vychází* z vrcholů $u$ a $v$. Na množinu vrcholů grafu $G$ odkazujeme jako na $V(G)$, na množinu hran $E(G)$.

**Stupněm vrcholu** $v$ v grafu $G$ rozumíme *počet hran vycházejících* z $v$. Stupeň $v$ v grafu $G$ značíme $d_{G}(v)$. Graf je **$d$-regulární**, pokud všechny jeho vrcholy mají *stejný stupeň* $d$. **Nejvyšší** stupeň v grafu $G$ značíme $\Delta(G)$ a **nejnižší** $\delta(G)$.

### Běžné typy grafů

**Kružnice** délky $n$ má $n \geq 3$ různých vrcholů spojených *do jednoho cyklu* $n$ hranami.

**Cesta** délky $n \geq 0$ má $n + 1$ různých vrcholů spojených *za sebou* $n$ hranami.

**Úplný graf $K_{n}$** na $n \geq 1$ vrcholech má $n$ různých vrcholů spojených *po všech dvojicích*, tj. celkem $\binom{n}{2}$ hran.

**Úplný bipartitní graf $K_{m, n}$** na $m \geq 1$ a $n \geq 1$ vrcholech má $m + n$ vrcholů *ve dvou skupinách* (partitách), přičemž hranami jsou spojeny *všechny $m \cdot n$ dvojice z různých skupin*.

**Hvězda** s $n \geq 1$ rameny je zvláštní název pro *úplný bipartitní graf $K_{1, n}$*.

### Podgrafy a isomorfismus

**Podgrafem** grafu $G$ rozumíme libovolný graf $H$ *na podmnožině vrcholů* $V(H) \subseteq V(G)$, který má za hrany **libovolnou podmnožinu hran** grafu $G$ *majících oba vrcholy ve $V(H)$*. Píšeme $H \subseteq G$.

**Indukovaným podgrafem** je podgraf $H \subseteq G$ takový, který obsahuje **všechny hrany** grafu $G$ *mezi dvojicemi vrcholů z $V(H)$*.

**Isomorfismus** $\simeq$ grafů $G$ a $H$ je *bijektivní zobrazení* $f: V(G) \to V(H)$, pro které každá dvojice $u, v \in V(G)$ je *spojená hranou v $G$* právě když je dvojice $f(u), f(v)$ *spojená hranou v $H$*. Grafy $G$ a $H$ jsou **isomorfní**, $G \simeq H$, pokud mezi nimi *existuje isomorfismus*.

### Souvislost grafů, komponenty

Mějme **relaci $\sim$** *na množině vrcholů* $V(G)$ libovolného grafu $G$ takovou, že pro dva vrcholy $x \sim y$ právě když *existuje v $G$ cesta začínající v $x$ a končící v $y$*. Pak $\sim$ je **relací ekvivalence**.

**Komponentami souvislosti** grafu $G$ nazveme **třídy ekvivalence** této *relace $\sim$*.

Graf $G$ je **souvislý**, pokud je $G$ tvořený **nejvýše jednou komponentou souvislosti**, tj. pokud *každé dva vrcholy* $G$ jsou *spojené cestou*.

### Stromy

**Strom** je jednoduchý *souvislý* graf $T$ **bez kružnic** (*acyklický*).

> [!note] Vlastnosti stromů
> Strom *s více než jedním vrcholem* obsahuje **vrchol stupně 1**.
> 
> *Strom na $n$ vrcholech* má **přesně $n - 1$ hran** pro $n \geq 1$.
> 
> Mezi *každými dvěma vrcholy stromu* vede právě **jediná cesta**.

**Les** je jednoduchý graf **bez kružnic** (*nemusí být souvislý*). Komponenty souvislosti lesa jsou stromy. Jeden vrchol bez hran a prázdný graf jsou také stromy.

### Sledy a tahy

**Sledem** délky $n$ v grafu $G$ rozumíme *posloupnost vrcholů a hran* $$(v_{0}, e_{1}, v_{1}, e_{2}, v_{2}, \dots, e_{n}, v_{n}),$$
ve které vždy hrana $e_{i}$ má koncové vrcholy $v_{i - 1}, v_{i}$.

**Tah** je sled v grafu bez opakování hran.

---

Graf $G$ **lze nakreslit jedním uzavřeným tahem** právě když $G$ *je souvislý* a *všechny vrcholy* v $G$ jsou **sudého stupně**.

Graf $G$ **lze nakreslit jedním otevřeným tahem** právě když $G$ je souvislý a *všechny vrcholy* v $G$ **až na dva** jsou *sudého stupně*.

## Procházení grafu

**Vstup**: *souvislý graf* $G$, daný seznamem vrcholů a seznamy vycházejících hran z každého vrcholu, plus případné ohodnocení.

1. Vybereme **libovolný počátek** prohledávání $u \in V(G)$; **úschovna** $U \leftarrow \{ u \}$.
2. Dokud $U \neq \emptyset$, **opakujeme**:
	1. **Zvolíme** $v \in U$; *odebereme* $U \leftarrow U \setminus \{ v \}$.
	2. Pokud $\text{stav}(v) = \text{zpracovaný}$, jdeme *zpět na začátek cyklu*.
	3. Volitelně provedeme **libovolnou akci** $\text{zpracuj}(v)$.
	4. **Pro všechny hrany** $f \in E(G)$ vycházející z $v$ *provedeme*:
		- Nechť $w$ je *druhý konec hrany* $f = vw$;
		- pokud $\text{stav}(w) \neq \text{zpracovaný}$, **odložíme** $U \leftarrow  U \cup \{ w \}$.
	5. $\text{stav}(v) \leftarrow \text{zpracovaný}$; jdeme *zpět na začátek cyklu*.
3. Souvislý $G$ je celý prohledaný a zpracovaný.

### Procházení do šířky (BFS)

Při **procházení do šířky** (*breadth first search*, *BFS*) je úschovna $U$ implementována jako **fronta**, neboli je voleno $v \in U$ od prvních vrcholů vložených do úschovny.

### Procházení do hloubky (DFS)

Při **procházení do hloubky** (*depth first search*, *DFS*) je úschovna $U$ implementována jako **zásobník**, neboli je voleno $v \in U$ od vrcholů později vložených do úschovny.

## Hledání nejkratší cesty

### Dijkstrův algoritmus

**Vstup**: *souvislý graf* $G$, daný seznamem vrcholů a seznamy vycházejících hran z každého vrcholu, plus *váhy* $w$ hran. *Počáteční vrchol* $u$ a *koncový vrchol* $v$.

1. **Úschovna** $U \leftarrow \{ (u, d(u) = 0) \}$.
2. Dokud $U \neq \emptyset$, **opakujeme**:
	1. **Zvolíme** $(x, d(x)) \in U$ takové, že $d(x)$ je *minimální*.
	   **Odebereme** $U \leftarrow U \setminus \{ (x, d(x)) \}$.
	2. Pokud $x = v$, algoritmus *může skončit*.
	3. **Pro všechny hrany** $f \in E(G)$ vycházející z $x$ *provedeme*:
		- Nechť $y$ je *druhý konec hrany* $f = xy$;
		  a nechť $d'(y) = d(x) + w(f)$
		  (*nová cesta do $y$ přes $x$*).
		- Pokud $(y, d(y)) \notin U$, nebo $(y, d(y)) \in U$ pro $d(y) > d'(y)$,
		  **odložíme** $U \leftarrow (U \setminus \{ (y, d(y)) \} \cup \{ (y, d'(y)) \})$
		  (*výměna za novou, lepší dočasnou vzdálenost do $y$*).

**Výstup**: $d(v)$ udává *váženou vzdálenost* z $u$ do $v$.

## Problém minimální kostry

**Minimální kostra** (*minimum spanning tree*, *MST*) je nejkratší spojení *mezi všemi vrcholy* grafu najednou.

*Podgraf* $T \subseteq G$ souvislého grafu $G$ se nazývá **kostrou**, pokud

- $T$ *je stromem* a
- $V(T) = V(G)$, neboli $T$ *propojuje všechny vrcholy* $G$.

**Váhou** (délkou) kostry $T \subseteq G$ váženého souvislého grafu $(G, w)$ rozumíme $$d_{G}^w(T) = \sum_{e \in E(T)} w(e).$$

**Problém minimální kostry** ve váženém grafu $(G, w)$ hledá kostru $T \subseteq G$ *s nejmenší možnou vahou* (přes všechny kostry grafu $G$).

### Hladový algoritmus

Mějme dán *souvislý vážený graf* $G$ s *ohodnocením hran* $w$.

1. **Seřadíme** *všechny hrany* $G$ jako $E(G) = (e_{1}, e_{2}, \dots, e_{m})$ tak, že $w(e_{1}) \leq w(e_{2}) \leq \dots \leq w(e_{m})$.
2. **Inicializujeme** *prázdnou kostru* $T = (V(G), \emptyset)$.
3. **Po řadě** pro $i = 1, 2, \dots, m$ *provedeme* následující:
	- Pokud $T + e_{i}$ **nevytváří kružnici**, tak $E(T) \leftarrow E(T) \cup \{ e_{i} \}$.
4. Na konci $T$ obsahuje *minimální kostru* grafu $G$.

### Jarníkův (Primův) algoritmus

**Vstup**: *souvislý graf* $G$, daný seznamem vrcholů a seznamy vycházejících hran z každého vrcholu, plus *váhy* $w$ hran.

1. Vybereme **libovolný počátek** prohledávání $v \in V(G)$; **úschovna** $U \leftarrow \{ (u, \emptyset) \}$.
2. Dokud $U \neq \emptyset$, **opakujeme**:
	1. **Zvolíme** $(x, e) \in U$ takové, že $w(e)$ je *minimální*.
	   **Odebereme** $U \leftarrow U \setminus \{ (x, e) \}$.
	2. **Přidáme** $E(T) \leftarrow E(T) \cup \{ e \}$ (*nová hrana do budoucí kostry*).
	3. **Pro všechny hrany** $f \in E(G)$ vycházející z $x$ *provedeme*:
		- Nechť $y$ je *druhý konec hrany* $f = xy$.
		- Pokud $(y, f') \notin U$, nebo $(y, f') \in U$ pro nějaké $w(f') > w(f)$,
		  **odložíme** $U \leftarrow (U \setminus \{ (y, f') \} \cup \{ (y, f) \}$.

**Výstup**: $T$ udává výslednou *minimální kostru*.

## Rovinné kreslení grafu

TODO