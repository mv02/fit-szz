#ifj, [[Ifj06-cz.pdf]]

![[21. Regulární jazyky a jejich modely#Základní pojmy]]

## Bezkontextová gramatika

**Bezkontextová gramatika** (*BKG*) je čtveřice $G = (N, T, P, S)$, kde:

- $N$ je *abeceda neterminálů*,
- $T$ je *abeceda terminálů*, přičemž $N \cap T = \emptyset$,
- $P$ je *konečná množina pravidel* tvaru $A \to x$, kde $A \in N, x \in (N \cup T)^\star$,
- $S \in N$ je *počáteční neterminál*.

### Derivační krok

Nechť $G = (N, T, P, S)$ je BKG. Nechť $u, v \in (N \cup T)^\star$ a $p = A \to x \in P$.

Potom $uAv$ **přímo derivuje** $uxv$ *za použití $p$* v $G$, zapsáno $$uAv \Rightarrow uxv\ [p]$$ nebo zjednodušeně $uAv \Rightarrow uxv$.

**Sekvence derivačních kroků**: $u_0 \Rightarrow^n u_n$, $u_0 \Rightarrow^\star u_n$, $u_0 \Rightarrow^+ u_n$ ($n$, 0+, 1+ derivací).

### Generovaný jazyk

Jazyk generovaný BKG je *množina řetězců terminálů* $w$, které **je možné v libovolném počtu kroků derivovat** z počátečního terminálu $S$.

Nechť $G = (N, T, P, S)$ je BKG.

**Jazyk generovaný** BKG $G$ je definován: $$L(G) = \left\{ w: w \in T^\star, S \Rightarrow^\star w \right\}.$$

### Bezkontextový jazyk

Nechť $L$ je jazyk. $L$ je **bezkontextový jazyk** (*BKJ*), pokud *existuje bezkontextová gramatika, která generuje tento jazyk* $L$.

### Nejlevější derivace

Během nejlevějšího derivačního kroku je přepsán **nejlevější neterminál**.

Nechť $G = (N, T, P, S)$ je BKG. Nechť $u \in T^\star$, $v \in (N \cup T)^\star$, $p = A \to x \in P$.

Pak $uAv$ přímo derivuje $uxv$ za pomocí **nejlevější derivace** užitím pravidla $p$ v $G$, zapsáno jako $$uAv \Rightarrow_\text{lm} uxv\ [p].$$

### Nejpravější derivace

Analogicky.

## Zásobníkový automat

**Zásobníkový automat** (*ZA*) je sedmice $M = (Q, \Sigma, \Gamma, R, s, S, F)$, kde:

- $Q$ je *konečná množina stavů*,
- $\Sigma$ je *vstupní abeceda*,
- $\Gamma$ je *zásobníková abeceda*,
- $R$ je *konečná množina pravidel* tvaru $Apa \to wq$, kde
	- $A \in \Gamma$ (symbol zásobníkové abecedy),
	- $p, q \in Q$ (stavy),
	- $a \in \Sigma \cup \{ \varepsilon \}$ (symbol vstupní abecedy nebo prázdný řetězec),
	- $w \in \Gamma^\star$ (řetězec zásobníkových symbolů nebo prázdný řetězec),
- $s \in Q$ je *počáteční stav*,
- $S \in \Gamma$ je *počáteční symbol na zásobníku*,
- $F \subseteq Q$ je množina *koncových stavů*.

> [!note] Grafická reprezentace zásobníkových automatů
>
> Nechť $p = Apa \to wq \in R$.
> Přijetí symbolu $a$, nahrazení symbolu $A$ na zásobníku řetězcem $w$: $$A/w, a$$
> Šipka ze stavu $p$ do stavu $q$ znázorňuje současný přechod mezi stavy.
> 
> ![[22_za-prechod.png]]

### Konfigurace zásobníkového automatu

Nechť $M = (Q, \Sigma, \Gamma, R, s, S, F)$ je ZA.

**Konfigurace ZA** $M$ je *řetězec* $\chi \in \Gamma^\star Q \Sigma^\star$.

(symboly na zásobníku + aktuální stav + symboly na vstupní pásce)

### Přechod zásobníkového automatu

Nechť $xApay$ a $xwqy$ jsou dvě konfigurace ZA $M$, kde $x, w \in \Gamma^\star$, $A \in \Gamma$, $p, q \in Q$, $a \in \Sigma \cup \{ \varepsilon \}$ a $y \in \Sigma^\star$. Nechť $r = Apa \to wq \in R$ je pravidlo.

Potom $M$ může provést **přechod** z $xApay$ do $xwqy$ *za použití $r$*, zapsáno $$xApay \vdash xwqy\ [r]$$ nebo zjednodušeně $xApay \vdash xwqy$.

**Sekvence přechodů**: $\chi_0 \vdash^n \chi_n$, $\chi_0 \vdash^\star \chi_n$, $\chi_0 \vdash^+ \chi_n$ ($n$, 0+, 1+ přechodů).
 
### Přijímaný jazyk

Nechť $M = (Q, \Sigma, \Gamma, R, s, S, F)$ je ZA.

**Jazyk přijímaný** ZA $M$ **přechodem do koncového stavu** je definován: $$L(M)_f = \{ w: w \in \Sigma^\star, Ssw \vdash^\star zf, z \in \Gamma^*, {\color{red} f \in F} \}.$$

**Jazyk přijímaný** ZA $M$ **vyprázdněním zásobníku** je definován: $$L(M)_\varepsilon = \{ w: w \in \Sigma^\star, Ssw \vdash^\star zf, {\color{red} z = \varepsilon}, f \in Q \}.$$

**Jazyk přijímaný** ZA $M$ **přechodem do koncového stavu a vyprázdněním zásobníku** je definován: $$L(M)_\varepsilon = \{ w: w \in \Sigma^\star, Ssw \vdash^\star zf, {\color{red} z = \varepsilon}, {\color{red} f \in F} \}.$$

> [!note] Ekvivalence přijímaných jazyků
> Existují algoritmy pro převody mezi ZA (nebo RZA), z nichž každý přijímá totožný jazyk jedním z výše uvedených způsobů.

## Deterministický zásobníkový automat

Nechť $M = (Q, \Sigma, \Gamma, R, s, S, F)$ je ZA.

$M$ je **deterministický zásobníkový automat** (*DZA*), pokud pro každé pravidlo tvaru $Apa \to wq \in R$ platí, že množina $R \setminus \{Apa \to wq\}$ *neobsahuje žádné pravidlo* s levou stranou $Apa$ nebo $Ap$.

> [!note] DZA vs. ZA
> *Zásobníkové automaty* jsou **silnější** než *deterministické ZA*.
> 
> Neexistuje žádný DZA $M_{f\varepsilon}$ přijímající jazyk $L = \{ xy: x, y \in \Sigma^*, y = \text{reversal}(x) \}$.

## Rozšířený zásobníkový automat

Z vrcholu zásobníku **lze číst celý řetězec** (v ZA to byl pouze jeden symbol).

**Rozšířený zásobníkový automat** (*RZA*) je sedmice $M = (Q, \Sigma, \Gamma, R, s, S, S, F)$, kde $Q$, $\Sigma$, $\Gamma$, $s$, $S$, $F$ jsou definovány stejně jako u ZA a $R$ je *konečná množina pravidel* tvaru ${\color{red}v}pa \to wq$, kde ${\color{red}v}, w \in {\color{red}\Gamma^*}$, $p, q \in Q$, $a \in \Sigma \cup \{ \varepsilon \}$.

> [!note] RZA vs. ZA
> *Rozšířené zásobníkové automaty* a *zásobníkové automaty* jsou **ekvivalentní**.
> 
> Pro každý RZA $M$ existuje takový ZA $M'$, pro který platí: $L(M)_f = L(M')_f$.

![[21. Regulární jazyky a jejich modely#Chomského hierarchie]]